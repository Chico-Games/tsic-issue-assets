# tsic-issue-assets

Screenshots and clips copied out of Discord bug and suggestion posts by the TSIC known-issues bot (`Tools/known-issues-sync`, `intake` command), so they keep rendering in tracker issues after the Discord CDN link expires.

Layout: `discord/<thread-id>/<n>-<original filename>`. Nothing here is hand-edited.
